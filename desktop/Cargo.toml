[package]
name = "enqueue-desktop"
version = "0.1.0"
edition = "2021"
description = "Enqueue"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
opt-level = "s"
lto = true
strip = true
