# Bookmark: CSS Grid layout reference

CSS Grid: container needs display: grid. Columns defined with grid-template-columns. Rows with grid-template-rows. Gap between items with gap. Items can span multiple columns or rows with grid-column and grid-row. The fr unit is fractional and fills remaining space. Auto-fit and minmax() together create responsive layouts without media queries.