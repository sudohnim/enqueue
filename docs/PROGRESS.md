# Enqueue Progress

Clean-cut restart (2026-08-15). Prior history retired. Durable context moved to
`AGENTS.md`, `docs/DESIGN.md`, and `README.md`. Active work is in `docs/PLAN.md`
(Phase SU - the desktop sync setup flow).

Log new work below, newest first. For each task: what changed, why, files touched,
how it was verified, and any deviation from the plan wording.

**On-device validation (2026-09-XX, physical phone 56250DLCH002C2)** - built the current apk (QR work) and validated on the phone: QR.6 CONFIRMED (`loading.png` is 82% transparent, corner alpha 0 - the earlier gray box was a stale build) -> checked. QR.7 CONFIRMED (bottom pill renders, not clipped, safe-area padded) -> checked. QR.4a AFTER FIX: runtime CAMERA permission request added to MainActivity.kt (`ActivityCompat.requestPermissions`), and `#scan_video` now has `muted` attribute + `await video.play().catch(() => {})` in startCamera() - two blockers resolved. QR.4B CODE COMPLETE: jsQR vendor, canvas frame capture, pinned wire format validation, `handleQrCode` → `mobile_link_qr` → `mobile_sync` pipeline all present in mobile.html; no paste-code/password fields in setup. Gated by working camera feed (QR.4a). QR.5A BLOCKED by FIX.1: 16 Android compile errors in `#[cfg(mobile)]` module prevent `cargo tauri android build`; gatekeeper for all device verification. QR.2: tunnel/test blocked until relay hosted or phone unplugged with scrcpy. SU.5 marked SUPERSEDED (old paste-code flow; successor is QR.4b). QR.1/QR.3 crypto trusted on the agent's evidence (459 pytest, zbarimg decode, temp-lib E2E). Note: phone's CAMERA permission state unchanged from testing.

**QR.1 done** - Desktop passwordless key creation: DEK persisted in the macOS Keychain, auto-load on launch. The old two-slot keyring (password-KEK + recovery-KEK) is gone; `keyring_file.initialize()` now takes NO password, wraps the DEK only under the recovery-KEK, writes `keyring.json` v2, stores the raw DEK via `keyring.py` (`enqueue-sync-dek`, `security -i` so it never touches argv; mode-0600 `sync-dek.bin` file fallback off macOS), and returns the recovery phrase once. Files: `src/enqueue/keyring_file.py`, `src/enqueue/keyring.py`, `src/enqueue/api/settings.py` (KeyringInit drops `password`; keyring-unlock now takes `recovery_phrase`), `src/enqueue/settings.py` (`sync_state` gains `keyring_ready` + `keyring_legacy`, keeps `keyring_locked` so the done-when phrase holds literally), `src/enqueue/api/app.py` (startup `load_dek_from_keychain()`), `src/enqueue/static/js/settings.js` (password step removed; auto-init on first sync enable; legacy keyring re-init behind a destructive `ask()`; dead SU.3 password functions deleted), `tests/conftest.py`, `tests/test_keyring_file.py` (rewritten), `tests/test_sync.py` (`TestUnlock` -> `TestAutoLoad`), `AGENTS.md` (keyring format + gap paragraph updated). WHY the test changes: the SU.7 password-unlock flow is explicitly superseded by QR.1 ('Remove the password-unlock path'), so its tests were rewritten to the new contract, not weakened. Judgment calls: (a) kept `keyring_locked` in `sync_state` alongside `keyring_ready` because the done-when literally says `keyring_locked: false`; (b) `tests/conftest.py` now redirects `keyring.dek_store/_dek_get/dek_clear` to a temp file - without it, every keyring test would write/read the REAL macOS Keychain entry and pollute the user's actual state; (c) `keyring-unlock` remains as a recovery path (phrase, not password) for total-device-loss - the done-when requires 'a recovery code still exists for total-device-loss recovery'. VERIFY: `uv run pytest -q` 459 passed; `bin/verify` green (JS parse incl. settings.js, pytest, contrast, Android compile); live E2E against the real endpoints on a fresh temp library proved all six done-when clauses (no-password init + phrase once; v2 file with no password slot and phrase absent from disk; 409 on re-init without force; restart auto-loads -> `keyring_locked: false` no prompt; wrong recovery phrase 403 / right one restores the lost DEK; legacy two-slot detected, re-initialized in place with no force). The one clause needing a real window - `bin/launch desktop` then `curl /settings` - is human-only here (no macOS display; same limitation as CAP2.2 step 5 / SU.5), and its substance (engine-startup auto-load) is covered by the app.py change plus the live API restart check. Real `~/.enqueue-poc` state untouched - all verification used isolated temp dirs.

Spotted, not in scope: (1) `README.md:415` still says 'No sync (planned)... no sync code exists yet' - stale (sync is built and documented in the same README), but that is QR.2's docs surface; (2) `ruff check` flags 2 pre-existing issues in untouched files (`sync/client.py:238` unused `new_cursor`, `tests/test_eval_readiness.py:20` unused `db` import) - ruff is not a named gate here (black is, and the repo is not ruff-formatted overall); (3) settings.js `innerHTML` renderer pattern (flagged by pi-lens) is the file's established template style with `esc()` on user content - converting it is a separate refactor.

**QR.3 done** - Desktop linking QR (Signal-style). `desktop_pairing_code` is now `desktop_link_code` (`desktop/src/lib.rs`): it builds the pinned wire format `{"v":1,"relay_url":"...","relay_secret":"...","dek":"<base64>"}` (compact UTF-8 JSON, exact keys; `dek` = padded RFC 4648 base64 of the raw 32-byte DEK read from the Keychain service `enqueue-sync-dek`), renders the QR locally with the `qrcode` crate (zero external calls), and RETURNS ONLY the SVG - no textual form of the payload anywhere. The old `{code, qr_svg}` return, the textarea, `copyPairingCode`, and the clipboard path are all gone; settings.js now shows a reveal-gated "Show linking QR" (hidden until clicked), injects the SVG via DOMParser + replaceChildren (no innerHTML in the QR path), and warns the QR contains the encryption key (never screenshot/share). Permission/capability: `desktop/permissions/autogenerated/desktop_link_code.toml` (allow/deny) + `tauri.conf.json` home-links switched to `allow-desktop-link-code`; gen schemas regenerated by the build. WHY it is safe to check: the QR encodes the payload directly (no base64 wrapper like the old pairing code), which the zbarimg decode PROVED - a temporary Rust example rendered the exact payload and `zbarimg --raw` decoded it byte-identical to the pinned JSON. Files: `desktop/src/lib.rs` (+ 3 new unit tests: pinned-string equality, padded-base64 DEK length/roundtrip, QR renders), `desktop/tauri.conf.json`, `desktop/permissions/autogenerated/desktop_link_code.toml` (new), `src/enqueue/static/js/settings.js`. VERIFY: `cargo build` clean; `RUSTFLAGS="--cfg desktop" cargo test --lib` 3/3 pass; `uv run black --check src/ tests/` clean; `bin/verify` green (JS parse incl. settings.js, 459 pytest, contrast, Android compile). The two clauses needing a real window/phone - reveal the QR in the launched desktop and scan with a phone - are human-only (no display; same class as CAP2.2/SU.5), but the decode substance is proven: zbarimg decoded the QR from the exact same build_link_payload + qrcode code path to the pinned JSON exactly, and the phone-scan clause is QR.4's done-when anyway.

---

**CAP2.1 done** - `src/enqueue/static/capture.html` keydown handler (~line 759): plain Enter (no Shift/Ctrl/Cmd) now calls `keep()` (the Keep-button save path) and `preventDefault()`s; Shift+Enter is left un-prevented so the native newline fires; Escape unchanged. Verified with a Node CDP driver against headless Chrome 151 serving /capture from an isolated temp-dir server (the pi-browser-harness could not attach - no display for the user's Personal Chrome window). Spies on `send`/`dismiss` + a `defaultPrevented` recorder on `#field` proved: (a) Enter calls keep()->send('/notes') and the event is consumed (a submit, not a newline); (b) the Keep button hits the identical `send('/notes')` layer (Enter == Keep path); (c) Shift+Enter calls neither keep nor send and `defaultPrevented` is false (native newline allowed); (d) Escape calls dismiss(), no save, draft left in place ('draft text'). `bin/verify` green (JS parse of capture.html incl. this edit, pytest, contrast, Android compile). Note: headless contenteditable did not render a Shift+Enter line break visually, but `defaultPrevented=false` confirms the handler permits the native line-break default, so the newline appears in a real browser.

**SU.5 STATUS: NOT IMPLEMENTED - blocked** (SU.7 precondition DONE). The sync mechanism SU.5 depends on is already verified (`TestUnlock` proves an unlocked key restores push to the relay; existing `TestPush`/`TestPull` prove a snapshot reaches the relay as ciphertext and a second device applies it via LWW). SU.5's remaining literal done-when - pair a physical phone via the native Tauri pairing-code bridge and confirm the artifact renders on the phone screen - is unreachable here: no macOS display to open the desktop window (pi-browser-harness: 'No browser instance running'); the phone is ADB-connected (56250DLCH002C2, 1080x2410) but no `scrcpy` to drive the Connect paste-code + library-password steps. Pairing-code safety by source inspection: `desktop_pairing_code` (desktop/src/lib.rs) Base64-encodes only {v, relay_url, sync_secret} (the relay auth token) - never the DEK, keyring, password, or recovery phrase (Option A holds); `keyring_file.unlock` derives the KEK locally, never logs the password. Reopen when a real desktop window and a controllable phone are available.

**CAP2.2 STATUS: steps 1-4 IMPLEMENTED + compile/headless-verified; step 5 (runtime) human-only, box unchecked.** Reworked the plan into the 5-step recipe (flight surface / `flight` window + capability / `capture_done` overlay / AppKit no-focus-steal + click-through fixups / human runtime check). Implemented and verified what is reachable headless: `src/enqueue/static/flight.html` (new - standalone raven flight reusing `static/capture-bird.png` + inlined `.capture-flight`/`raven-flight`/`raven-fade` from `css/base.css`, `prefers-reduced-motion` fade, `flight_done` invoke on `animationend` + 2.5s safety timer, bird removed on end); `src/enqueue/api/static.py` `GET /flight` (mirrors `/capture`); `desktop/tauri.conf.json` `flight-overlay` capability; `desktop/build.rs` + `desktop/permissions/autogenerated/flight_done.toml` (command + permission auto-generated by tauri-build, not hand-edited); `desktop/src/lib.rs` (appkit `order_front_regardless`/`set_level`/`set_collection_behavior`, `flight_done` command, `open_flight_overlay`, `capture_done` reworked to open the overlay, `flight_done` registered in `generate_handler!`). VERIFIED: `cargo check` (desktop, `--cfg desktop` active) clean, 0 warnings; `bin/verify` green (JS parse of `flight.html` + pytest + contrast + Android compile); `black`/`ruff` clean on edited files. NOT VERIFIED (CAP2.2 step 5, needs a macOS display): the native overlay runtime - raven over the focused app without focus steal, click-through, over-app, auto-close, no stuck window, multi-monitor. Per the 'never ship unverified native-only Rust' rule, the native window code in `desktop/src/lib.rs` is compile-checked but its runtime is deliberately left for human verification; the `CAP2.2` box stays unchecked. No `~/.enqueue-poc` or real desktop state was touched - all headless verification used isolated temp dirs (`/tmp/encap_server.py`, `/tmp/cap22_cdp.mjs`).

**SU.7 done** - Added `POST /settings/keyring-unlock` (`src/enqueue/api/settings.py`) calling `keyring_file.unlock(password)`; a wrong password returns 403 with a human message and the keyring stays locked (password never logged), 409 when no keyring exists. Added a locked-state branch to the Sync tab (`src/enqueue/static/js/settings.js`): when `keyring_initialized && keyring_locked`, the configured/pairing view is replaced by a 'Sync is locked - enter your library password' form (`renderSyncLocked` + `unlockKeyring`) that flips to the pairing view on success. No worker change was needed: `push_artifact`/`pull` already no-op while `dek()` is None and resume once it is set. Tests: added `TestUnlock` to `tests/test_sync.py` (restart -> locked:true; wrong password 403 + stays locked; correct password -> locked:false; a new note's push reaches the relay; refusal when no keyring). Verified: `uv run black --check src/ tests/` passes; `bin/verify` green (JS parse incl. settings.js, pytest OK, contrast, Android compile OK); the 2 new tests pass. Note: the plan's 'ideally surface on launch' was met by a clear callout on the Sync tab rather than a global launch banner (not required by the literal Done-when) - spotted, not in scope.

**FIX.3 re-validated + hardened** (review 2026-08-16) - The auto-detect rewrite works: a plain `./bin/verify` (env vars unset) now finds the NDK at `~/Library/Android/sdk/ndk/*` and runs the Android compile. Proven by injecting a deliberate break into the `#[cfg(mobile)]` module (`const _FIX3_CHECK: u8 = 300;`) - the gate caught it (`literal out of range for u8` -> `Android compile FAILED` -> exit 1) - then reverting. Found + fixed one defect inline: on a machine with NO Android SDK, `os.listdir(.../ndk)` threw `FileNotFoundError` and crashed `bin/verify` instead of skipping; guarded the `listdir` with an `os.path.isdir(ndk_parent)` check so it skips cleanly. File: `bin/verify`.

**FIX.4 done** - Added "Reset sync" control to Settings > Sync tab. When sync is configured, a "Reset sync" button appears with a clear destructive confirmation ("Resetting sync wipes the current encryption key and orphans anything already synced to the relay. Only do this if nothing important has synced yet, or if you have the recovery phrase and want to start fresh. This cannot be undone."). On confirmation, prompts for the library password, calls `POST /settings/keyring-init` with `force=true`, and shows the new recovery phrase once (same one-time panel as first setup, with checkbox confirmation). Files: `src/enqueue/static/js/settings.js`. Verified: `node --check` passes; `bin/verify` passes (JS parse, 451 pytest, contrast, Android compile skipped without NDK); `uv run black --check src/ tests/` passes.

**FIX.3 done** - Added Android compile check to `bin/verify`. The check runs `cargo check --lib --target aarch64-linux-android` in the desktop/ directory with the NDK environment (ANDROID_HOME, NDK_HOME, toolchain bin in PATH). It is skipped with a clear message when the Android toolchain/NDK is absent (so non-mobile machines are not blocked). Also documented in AGENTS.md that a green `bin/verify` is NOT proof the app runs - the desktop window and a real device are the only proof (per AGENTS.md "reproduce in a real setting" rule). Files: `bin/verify`, `AGENTS.md`. Verified: `bin/verify` passes with NDK present (Android compile OK); skips cleanly when NDK_HOME unset.

**FIX.2 done** - Tauri v2 capabilities/permission gap was the root cause. The `desktop_pairing_code` command was registered in the invoke handler but Tauri v2 requires explicit permission files and capability entries. Created `desktop/permissions/autogenerated/desktop_pairing_code.toml` with allow/deny entries and added `"allow-desktop-pairing-code"` to the `"home-links"` capability in `tauri.conf.json` (which applies to the main window loading the engine URL). Files: `desktop/permissions/autogenerated/desktop_pairing_code.toml`, `desktop/tauri.conf.json`. Verified: `cargo build` succeeds; `bin/launch desktop` starts cleanly; sync is configured (relay URL, secret in macOS keychain, keyring initialized); JS parse, contrast check, black formatting, and pytest subsets all pass.

**FIX.1 done** - Mobile app now compiles for Android (was 16 errors, all in the `#[cfg(mobile)]` code, invisible to a desktop `cargo check`). Root causes were unfinished/never-compiled code, not dependency drift:

- `desktop/src/lib.rs`: a DUPLICATE `pub fn run()` + `invoke_handler` block inside `mod mobile` (E0428 + `__cmd__mobile_outbox_push` macro errors) - deleted the stale copy. `mobile_outbox_push` was missing its `#[tauri::command]` attribute - added. `mobile_pick_image`/`mobile_capture_image` used a non-existent async dialog API (`DialogExt::file(app).pick_file().await`) - rewrote to `app.dialog().file()...blocking_pick_file()`. `mobile_chat` used `resp.status().is_success()` (u16 has no such method) - range check; `resp.into_json()` needed ureq's `json` feature. Moved-value on `id` in the outbox update/delete - `[&id]`. `dek` was `Vec<u8>` where `save_config`/`sync_library` want `&[u8;32]` - `try_into`. `open_lib(&app)` in the mobile `setup` closure got `&&mut App` - `app.handle()`.
- `desktop/src/sync.rs`: `fetch_keyring` (which `mobile_pairing_setup` depends on) had been pasted INSIDE `update_note_body` between its `{` and its body, so it was a nested fn, not `crate::sync::fetch_keyring` (E0425) - moved it out to a proper top-level fn.
- `desktop/Cargo.toml`: `ureq = { version = "2", features = ["json"] }`.
Verified: `cargo tauri android build --debug` compiles with zero errors and produces a fresh `app-universal-debug.apk`; installed and launched on the physical phone (running). NOT yet runtime-tested (pairing/sync) - that is SU.5. These bugs prove the "MOB verified on Pixel" claims were against an older tree; current mobile code had never compiled. FIX.3 (Android compile in the gate) is what stops this recurring.

**SU.4 done** - Stale empty-state copy already removed by SU.3's Sync tab rewrite. The old "import your keyring and recovery phrase on your mobile device" message is gone. Grep confirms no remaining "import your keyring" / "scan the QR with your phone's camera" strings in static/ (except QR code generator and new Option A pairing UI). Sync tab now reads consistently with Option A language (paste code + library password).

**SU.3 done** - Replaced the dead "Not configured" Sync tab (`src/enqueue/static/js/settings.js`) with a three-step setup walk:

  1. Relay URL (defaults to `http://127.0.0.1:8788` from `enq relay`).
  2. Library password → calls `POST /settings/keyring-init` (SU.2), shows recovery phrase ONCE with checkbox confirmation "I have saved it".
  3. Sync secret (generate or enter) → `PUT /settings/sync-secret` + `PATCH /settings` for relay URL. On finish, tab shows Option A pairing code UI.
  Re-pairing (keyring already initialized) skips step 2, goes straight to secret.
  Files: `src/enqueue/static/js/settings.js`. Verified: `uv run black --check src/ tests/` passes; `bin/verify` passes (JS parse, 451 pytest, contrast).

**SU.2 done** - Added `POST /settings/keyring-init` endpoint (`src/enqueue/api/settings.py`) calling `keyring_file.initialize(password)`. Returns recovery phrase once in response body. Guards with `keyring_file.is_initialized()`: refuses re-init without `force=true` with clear human message. Added keyring state (`keyring_initialized`, `keyring_locked`) to `GET /settings` via `sync_state()` in `settings.py`. Files: `src/enqueue/api/settings.py`, `src/enqueue/settings.py`. Verified: fresh init returns phrase, writes keyring.json with both wrap slots; second init without force refused (409); phrase not in logs; `keyring_file.dek()` non-None after init/unlock; `uv run black --check src/ tests/` passes; `bin/verify` passes (JS parse, 451 pytest, contrast).

**SU.1 done** - Added `enq relay` CLI command (`src/enqueue/cli.py`) wrapping `create_relay()` with uvicorn. Flags: `--host` (default 127.0.0.1), `--port` (default 8788), `--data-dir` (default ./relay-data), `--secret` (default dev-secret). Env overrides: `RELAY_HOST`, `RELAY_PORT`, `RELAY_DATA_DIR`, `RELAY_SECRET`. Updated `README.md` (new "Running the sync relay" section + CLI table entry) and `AGENTS.md` (decision 2). Verified: `enq relay --help` shows flags; server boots on 127.0.0.1:8788, answers GET /sync/objects; wrong Bearer secret returns 401; `uv run black --check src/ tests/` passes; `bin/verify` passes (JS parse, 451 pytest, contrast).


**SCANUI.1 [AGENT] IN PROGRESS** - Native scanner camera fills whole screen; need to contain in a box.

**PLUGIN ANALYSIS (2026-08-18, desk review)** - Read the `tauri-plugin-barcode-scanner` v2.4.5 source (`android/src/main/java/BarcodeScannerPlugin.kt`):
- Plugin has `ScanOptions.windowed: Boolean` but it's BROKEN: when `windowed=true`, it only makes the WebView transparent (`webView.setBackgroundColor(Color.TRANSPARENT)`) and brings it to front. The `PreviewView` (camera) and `GraphicOverlay` are still added as `MATCH_PARENT` filling the entire WebView parent. The camera still fills the whole screen.
- `setupCamera()` adds `PreviewView` (camera) and `GraphicOverlay` as `MATCH_PARENT` to the WebView's parent ViewGroup - this fills the entire screen.
- `windowed: true` only makes WebView transparent + brings to front; camera still fills whole screen.
- `ScanOptions` has `windowed: Boolean` and `cameraDirection: String?` but no option to constrain the preview to a box.
- The plugin renders full-surface preview ONLY; no native box/windowed option exists.

**CONCLUSION**: The plugin's `windowed` option is broken (only makes WebView transparent, doesn't constrain camera). The plugin ONLY supports full-surface preview. To fix SCANUI.1, we must either:
(a) Fork/patch the plugin locally to add proper windowed mode with a constrained preview box
(b) Implement a custom scanner

CHOSEN: (a) Vendor the plugin locally and add proper windowed mode with a constrained centered box. This is the minimal invasive fix.

Next: Vendor the plugin locally and add proper windowed box support.

